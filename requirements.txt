# ============================================================
# NeuralForge Requirements
# Optimized for: RTX 5090 32GB | i9-284K | 128GB DDR5 | CUDA 12.x
#
# ⚠️  WINDOWS INSTALL ORDER MATTERS — use setup_windows.ps1
#     or follow the manual steps below.
#
# Manual install order:
#   1. pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   2. pip install torch-geometric
#   3. pip install pyg-lib torch-scatter torch-sparse torch-cluster torch-spline-conv -f https://data.pyg.org/whl/torch-2.3.0+cu121.html
#   4. pip install -r requirements.txt
# ============================================================

yfinance>=0.2.40

# --- Core Deep Learning ---
# Install these FIRST via the index URL above (CUDA wheels):
# torch>=2.3.0
# torchvision>=0.18.0
# torchaudio>=2.3.0

# --- PyTorch Geometric (core — no binary extensions here) ---
torch-geometric>=2.5.0
# Binary extensions (torch-scatter, torch-sparse, torch-cluster, torch-spline-conv)
# must be installed via: pip install ... -f https://data.pyg.org/whl/torch-2.3.0+cu121.html
# See scripts/setup_windows.ps1 for the automated one-shot setup.

# --- ONNX ---
onnx>=1.16.0
onnxruntime-gpu>=1.18.0
onnxscript>=0.1.0

# --- AI Assistant ---
anthropic>=0.28.0

# --- Web Framework ---
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
eventlet>=0.35.0

# --- Visualization ---
plotly>=5.20.0
matplotlib>=3.8.0
seaborn>=0.13.0
networkx>=3.3.0

# --- GPU Monitoring ---
pynvml>=11.5.0
psutil>=5.9.0
GPUtil>=1.4.0

# --- Data & Utils ---
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
tqdm>=4.66.0
rich>=13.7.0
click>=8.1.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0

# --- Jupyter ---
jupyter>=1.0.0
ipywidgets>=8.1.0
ipykernel>=6.29.0

# --- Dev / Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
ruff>=0.4.0
